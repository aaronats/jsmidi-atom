'use babel';

import { expect } from 'mocha';

// TODO: I suck at UI tests so leaving this for later.

describe('JSMidiAtom', function () {
  it('should have tests', function () {
    expect('there should be tests').to.eq('there should be tests');
  });
});
